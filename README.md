# becanda - Slot Demo (backend)

Run:

```
cd backend
npm install
cp .env.example .env
# edit .env JWT_SECRET
node server.js
```

Server listens on http://localhost:4000
Default admin created: username=admin password=admin123 (change it)